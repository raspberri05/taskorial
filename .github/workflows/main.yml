# .github/workflows/netlify.yml
name: Build and Deploy to Netlify
on:
  release:
    types: [published]
    branches: main
jobs:
  build:
    runs-on: ubuntu-latest
    environment:
      name: netlify-deploy
      url: https://to-do-list-05.netlify.app/
    steps:
      - uses: actions/checkout@v3
      - name: "deployed site"
        run: curl -X POST -d {} ${{ secrets.NETLIFY_BUILD_HOOK }}
      
